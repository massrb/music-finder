<p style="color: green"><%= notice %></p>

<% content_for :title, "Emails" %>

<h1>Emails</h1>

<div id="emails">
  <% @emails.each do |email| %>
    <p>
      <%= link_to email.title, email %>
    </p>
  <% end %>
</div>

<%= link_to "New email", new_email_path %>
